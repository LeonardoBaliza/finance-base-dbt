[project]
name = "acadia"
version = "0.1.0"
description = "Finance Base dbt mini project for Snowflake"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dbt-core>=1.8,<2.0",
    "dbt-snowflake>=1.8,<2.0",
]
